---
  hooks:
    bitbucket.org:
      type: bitbucket
      path: /payload
      timeout: 600
      cmd:
        - echo
        - '{{.Branch}}'
    github.com:
      type: github
      path: /github
      cmd:
        - echo
        - '{{.Branch}}'

